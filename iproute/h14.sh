#!/bin/sh

ip route add 10.0.0.1 dev h14-eth0 via 10.0.0.14 realm 2
ip route add 10.0.0.2 dev h14-eth0 via 10.0.0.14 realm 3
ip route add 10.0.0.3 dev h14-eth0 via 10.0.0.14 realm 4
ip route add 10.0.0.4 dev h14-eth0 via 10.0.0.14 realm 5
ip route add 10.0.0.5 dev h14-eth0 via 10.0.0.14 realm 6
ip route add 10.0.0.6 dev h14-eth0 via 10.0.0.14 realm 7
ip route add 10.0.0.7 dev h14-eth0 via 10.0.0.14 realm 8
ip route add 10.0.0.8 dev h14-eth0 via 10.0.0.14 realm 9
ip route add 10.0.0.9 dev h14-eth0 via 10.0.0.14 realm 10
ip route add 10.0.0.10 dev h14-eth0 via 10.0.0.14 realm 11
ip route add 10.0.0.11 dev h14-eth0 via 10.0.0.14 realm 12
ip route add 10.0.0.12 dev h14-eth0 via 10.0.0.14 realm 13
ip route add 10.0.0.13 dev h14-eth0 via 10.0.0.14 realm 14
ip route add 10.0.0.15 dev h14-eth0 via 10.0.0.14 realm 15
ip route add 10.0.0.16 dev h14-eth0 via 10.0.0.14 realm 16
